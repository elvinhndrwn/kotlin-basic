fun main() {
    /**
     * gunakan tanda .. untuk naik dan downTo untuk turun
     * tidak digunakan utk menampung data
     * digunakan ketika looping
     */

    val range = 0..100
    val range2 = 10 downTo 1 step 2

    println(range.count())
}